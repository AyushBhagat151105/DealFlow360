# Docker Compose for DealFlow360
# Full-stack container orchestration

name: DealFlow360

services:
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: DealFlow360-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - VITE_SERVER_URL=http://localhost:3001
    restart: unless-stopped
    depends_on:
      - server
    networks:
      - DealFlow360-network

  server:
    build:
      context: ./apps/server
      dockerfile: Dockerfile
    container_name: DealFlow360-server
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=postgresql://postgres:password@db:5432/DealFlow360
      - BETTER_AUTH_SECRET=dealflow360_super_secret_auth_key_production_32chars!
      - BETTER_AUTH_URL=http://localhost:3001
      - CORS_ORIGIN=http://localhost:3000
    restart: unless-stopped
    depends_on:
      - db
    networks:
      - DealFlow360-network

  db:
    image: postgres:16-alpine
    container_name: DealFlow360-postgres
    environment:
      POSTGRES_DB: DealFlow360
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - DealFlow360_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - DealFlow360-network

networks:
  DealFlow360-network:
    driver: bridge

volumes:
  DealFlow360_postgres_data:
